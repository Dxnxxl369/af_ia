name: movil # Usamos el nombre 'movil' de tu archivo default
description: "Aplicación móvil para gestión de activos fijos."
publish_to: 'none' # Añadido de tu archivo default
version: 1.0.0+1

environment:
  sdk: '^3.8.1' # Usamos la versión SDK más nueva de tu archivo default

dependencies:
  flutter:
    sdk: flutter

  # --- Paquetes Actuales ---
  lucide_flutter: ^0.548.0 # <-- VERSIÓN CORREGIDA (la que sugirió el error)
  provider: ^6.1.1
  http: ^1.1.0
  shared_preferences: ^2.2.2
  flutter_colorpicker: ^1.0.3
  jwt_decode: ^0.3.1

  # --- Paquetes Futuros ---
  image_picker: ^1.0.4
  cached_network_image: ^3.3.0
  speech_to_text: ^6.5.1

  # Iconos predeterminados (usamos la versión de tu default)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # Usamos la versión de lints de tu default

flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg